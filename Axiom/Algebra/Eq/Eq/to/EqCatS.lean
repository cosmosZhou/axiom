import Axiom.sympy.tensor.Tensor

namespace Algebra.Eq.Eq.to



-- theorem EqConcats
--   {n m k : ℕ}
--   {f g : ℕ → α}
-- -- given
--   (h1: x = y)
--   (h2: a = b) :
-- -- imply
--   Lambda (n + 1) f = Lambda (n + 1) g :=
-- -- proof
-- by
--   calc
--     Lambda (n + 1) f = append (Lambda n f) (f n) := by apply lambda_to_append
--     _ = append (Lambda n g) (g n) := by rw [h1, h2]
--     _ = Lambda (n + 1) g := by apply lambda_to_append.symm

end Algebra.Eq.Eq.to

-- created on 2024-07-01
